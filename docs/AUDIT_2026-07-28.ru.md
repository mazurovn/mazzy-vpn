# Аудит Mazzy VPN — 2026-07-28

Copyright © 2026 [Nik m (@mazurovn)](https://github.com/mazurovn).

## Вердикт

Текущий `main` нельзя считать готовым к production-релизу Desktop или mobile.
CLI/TUI 1.2.0 остаётся единственным стабильным релизом, но защищённый local API
и его клиенты уже слиты через
[#26](https://github.com/mazurovn/mazzy-vpn/pull/26) и
[#27](https://github.com/mazurovn/mazzy-vpn/pull/27). Это устраняет прежний
Draft-разрыв, но не закрывает перечисленные ниже production gates.

Linux Desktop 0.2.0 — preview. Windows и macOS артефакты — неподписанные UI
preview без VPN backend. Исходников Android/iOS приложений в репозитории нет.
Выпускать эти четыре платформы как работающий VPN сейчас нельзя.

Аудит охватывает Bash engine/installer, systemd local API, CLI/TUI, Rust/Tauri
Desktop adapter, GitHub Actions, release artifacts, dependency chain,
capability registry и планы Windows, macOS, Android и iOS.

## Исправлено после аудита

Ниже перечислены не релизные обещания, а подтверждённые дефекты. Исправления
из #26/#27 находятся в `main`; package-lifecycle slice должен пройти отдельные
review, CI и merge до включения в baseline.

| Важность | Дефект | Исправление |
|---|---|---|
| высокая | Прерванный API action навсегда оставался `running`, блокировал следующие мутации и не восстанавливал состояние | Snapshot создаётся до `running`; orphan action под mutation lock переводится в терминальное состояние с rollback |
| высокая | API читал строку запроса без ограничения памяти; длина считалась символами, а не байтами | Чтение ограничено байтами до JSON parsing, добавлен конечный receive timeout и предел 1 MiB |
| высокая | После потерянного ответа Desktop не повторял тот же idempotent action | Ровно один retry разрешён только для `Indeterminate`, с тем же request/action ID; первоначальный `Unavailable` не повторяется |
| высокая | `status --json` и `profiles --json` меняли опубликованную schema при наличии API socket | Legacy schema v1 сохранена; raw envelope доступен только через явный `--api-json` |
| высокая | Управляющие символы из вручную установленного имени профиля могли попасть в терминал | Все control characters отфильтровываются до CLI/TUI output и при импорте |
| высокая | Root installer собирал AmneziaWG из tag без проверки immutable commit | После shallow clone проверяется точный upstream commit; несовпадение останавливает сборку |
| высокая | Desktop сначала целиком накапливал stdout/stderr дочерних процессов и только затем обрезал результат | Оба потока дренируются параллельно; в памяти сохраняется не более 512 KiB с явной меткой усечения |
| средняя | API-backed TUI терял interface, public IP, handshake, autostart, monitor и fallback | `status.get` и terminal render получили необязательные безопасные поля; VPN endpoint остаётся запрещён |
| средняя | Новые API/client ошибки были захардкожены по-русски | Selection, transport, mutation и status messages локализованы и проверены для ru/en/de/zh/ja/ko |
| средняя | Action journal и audit log росли без границ | Action records ограничены, audit log ротируется с root-only правами |
| средняя | JSON-валидаторы допускали неоднозначные duplicate keys | Contract и capability validators используют strict duplicate-key parsing |
| высокая | Runtime dispatcher принимал неоднозначный request с duplicate keys или несколькими top-level JSON documents | До dispatch принимается ровно один strict JSON object; escaped duplicate keys также отклоняются |
| высокая | Timeout завершал только верхний shell, а дочерний `systemctl` мог продолжить мутацию после ответа `timed-out` | Executor и rollback запускаются в отдельной process group; fault-test проверяет отсутствие живого потомка |
| высокая | Ошибка записи start/terminal audit не останавливала mutation и могла потерять уже изменившееся состояние | Start audit обязателен до executor; terminal audit failure сохраняет idempotent outcome и recovery marker |
| высокая | Query refresh не имел общего предела, а аварийное завершение оставляло временные cache-файлы | Refresh ограничен оставшимся deadline и process group; временные файлы уникальны и очищаются |
| высокая | CLI и Desktop принимали неоднозначный transport response; CLI зависел от locale при byte limit и скрывал action ID в части ошибок | Оба клиента принимают ровно один envelope до bounded EOF; CLI считает байты в `LC_ALL=C`, query отправляет deadline, mutation failure всегда показывает action ID |
| средняя | GitHub checkout сохранял credential helper в Desktop jobs | `persist-credentials: false` установлен во всех checkout |
| средняя | Desktop CI использовал `macos-latest`, `windows-latest` и Rust `stable` | Поколения runner закреплены на Ubuntu 22.04/24.04, macOS 14 и Windows Server 2022; Rust и локальный toolchain закреплены на 1.85.0 |
| средняя | Runtime caches с mode `0644` раскрывали public IP и profile labels всем локальным аккаунтам | Каталог публикуется как `0750 root:mazzy-vpn`, файлы — `0640`; при отсутствии группы применяется fail-closed `0700/0600` |
| высокая | При missing/failed rollback snapshot следующий API action снова разрешался | Root-only recovery marker блокирует все последующие mutations до ручного ремонта и явного administrator acknowledgement под mutation lock |
| высокая | Installer менял root-owned файлы до успешной установки зависимостей | Порядок изменён на preflight → зависимости → deferred tests → файлы; regression проверяет порядок |
| средняя | Socket parent создавался как `0755`, а Desktop не замечал отсутствие `socat` | tmpfiles создаёт `/run/mazzy-vpn` как `0750 root:mazzy-vpn`; dependency report включает `socat` |
| средняя | DEB/RPM не гарантировали наличие `pkexec`, поэтому встроенный bootstrap мог вообще не запуститься | DEB требует `pkexec`, RPM — `polkit`; фактические package metadata и сохранение WebKit/GTK dependencies проверены |
| высокая | DEB/RPM не владели engine/systemd runtime: после установки GUI требовался второй privileged flow, а uninstall не знал о core-файлах | Package payload владеет `/usr/bin`, `/usr/lib/mazzy-vpn`, units/drop-ins и tmpfiles; lifecycle scripts проверяют contract, активируют systemd и не владеют `/etc/vpnctl` |
| высокая | Package Repair мог запустить embedded installer и создать вторую копию engine в `/usr/local`, расходящуюся с package manager | Desktop выбирает `/usr/bin` раньше `/usr/local`; для package-managed установки Repair выполняет только `mazzy-vpn doctor --fix` |
| высокая | AppImage embedded installer запускал regression suite, но bundle не содержал Desktop config/UI, package lifecycle sources и wiki, на которые suite и capability registry ссылаются | AppImage теперь содержит проверяемый self-contained source set; release audit распаковывает SquashFS, сверяет byte identity, запускает contract/capability validators и staged installer |
| высокая | Base package metadata не гарантировала `procps`/`procps-ng`, хотя process-group cleanup использует `ps`; TCP probe tool также выпадал из package flow | `procps`/`procps-ng` стали жёсткими dependencies, `netcat-openbsd`/`nmap-ncat` — recommendations; фактическая metadata проверяется после сборки |
| средняя | Графический package manager мог не передать postinstall UID пользователя, оставляя его вне группы local API | Package-safe Repair повторно определяет `PKEXEC_UID` и добавляет вызывающего пользователя в `mazzy-vpn`; новый login всё ещё обязателен для обновления групп сеанса |
| средняя | Локальный release build сохранял старые bundles, которые могли попасть в общий artifact glob; повторная упаковка уже пропатченного Tauri executable теряла исходный bundle marker | `build:release` удаляет предыдущий `target/release/bundle` и верхнеуровневый executable, вынуждая Cargo восстановить marker; package audit требует ровно по одному AppImage, DEB и RPM |
| высокая | Tag workflow собирал release без Rust unit tests/Clippy, а release text ссылался на несуществующую checksum chain | Release job запускает 15 unit tests и Clippy; ложное обещание checksum удалено, unsigned status указан явно |
| средняя | Активный Desktop toggle «Уведомления» ничего не менял, кроме неиспользуемого `localStorage` | Фиктивный handler удалён; control отключён и честно помечен как недоступный в preview |

## Незакрытые технические риски

### P0 — блокируют production

1. **Нет общего native core/service.** `mazzy-vpn` — root-executed Bash-монолит
   более чем на 5 500 строк. Local API реализует только status, profiles и
   lifecycle на Linux; остальные Desktop операции продолжают использовать
   `pkexec`. Это не переносимая архитектура Windows/macOS/mobile.
2. **Нет идентификации вызывающего процесса.** Socket ограничен группой
   `mazzy-vpn`, но Bash dispatcher не получает `SO_PEERCRED`. Audit не содержит
   достоверный UID/PID caller. Нужен native daemon, проверяющий peer credentials
   и policy для каждой операции.
3. **Windows/macOS не поднимают туннель.** Сборка Tauri доказывает только
   компиляцию UI. Нет Windows Service/Wintun backend, Network Extension,
   platform secret storage, подписания, notarization и functional VPN tests.
4. **Android/iOS отсутствуют.** Нет Gradle/Xcode проектов, `VpnService` или
   Packet Tunnel provider, mobile signing, real-device tests и store metadata.
5. **Нет доверенной release chain.** Артефакты preview не подписаны. Нет SBOM,
   provenance/attestation, обязательной проверки подписей обновления и
   проверенного rollback. Checksums без защищённой подписи не подтверждают
   издателя.
6. **Нет clean-device/system integration gates.** Unit/regression tests
   используют fake environment. Не проверены реальные systemd socket
   permissions, upgrade/uninstall, route/DNS leak, crash во время операции,
   sleep/resume, смена сети и восстановление после reboot.

### P1 — должны быть закрыты до Linux Desktop 1.0

1. `deadline_ms` теперь вычитает mutation preflight из монотонного бюджета,
   сохраняет миллисекундную точность и не запускает executor после истечения.
   Но это всё ещё не строгий deadline всего request/response: чтение и
   envelope validation происходят до старта бюджета, а обязательные
   crash-reconciliation/rollback используют отдельный ограниченный completion
   grace. Общего безопасного cancel для всего service request пока нет.
2. После удаления старой idempotency record из ограниченного журнала очень
   старый `action_id` может выполниться снова. При текущем произвольном
   Identifier точная бесконечная история несовместима с bounded storage:
   нужен новый согласованный контракт с server-issued token/expiry или
   проверяемым issued-at, а не только увеличение retention.
3. Desktop читает отдельные group-restricted status/profile caches для части
   query и переводит через API только lifecycle. Doctor, services, import,
   tests и modes ещё не мигрированы; единая policy/data boundary отсутствует.
4. Compatibility child processes не имеют общего внешнего deadline/cancel.
   Память вывода теперь ограничена, но зависший `pkexec`/helper всё ещё может
   удерживать Desktop worker. Слепой kill после начала мутации недопустим:
   нужны action ID, reconciliation и явно определённый indeterminate outcome.
5. Полной воспроизводимости release build всё ещё нет: даже versioned GitHub
   runner labels получают обновления образов, Node задан только major-версией,
   а пакеты `apt` не закреплены. Rust 1.85.0 и поколения runner уже
   зафиксированы, но hermetic builder и сравнение бинарной воспроизводимости
   отсутствуют.
6. Dependency bootstrap остаётся частичным. DEB/RPM теперь устанавливают
   package-owned engine/service и обязательный base runtime; protocol packages
   объявлены recommendations, а явный `doctor --fix` остаётся для
   недоступных/AmneziaWG зависимостей. AppImage не может запустить embedded
   installer, если `pkexec` отсутствовал на host изначально.
7. Package lifecycle имеет идемпотентные install/upgrade/remove scripts и
   сохраняет `/etc/vpnctl`/`/var/lib/vpnctl`, но ещё нет clean-device VM matrix
   для всех дистрибутивов, fault injection postinst, package-manager rollback и
   проверки сложных legacy/custom unit overrides. Поэтому issue #4 и gate
   `desktop-linux-1.0` остаются открытыми.

### P2 — качество и сопровождение

1. Есть точечные fault-regressions для deadline и failed rollback, но нет
   fuzz/slowloris/soak тестов local API и длительного fault-injection.
2. Нет `cargo audit`/`cargo deny`, автоматического SBOM и подписи provenance.
   Lockfiles и Dependabot есть, но этого недостаточно.
3. `npm audit --audit-level=high` на 2026-07-28 сообщил 0 известных
   vulnerabilities. Это point-in-time registry check: он не покрывает Rust,
   системные packages, malicious maintainer update или будущие advisories.
4. API contract содержит 25 операций, но большинство transport handlers ещё
   отсутствует. Статус `partial` должен сохраняться до фактической реализации.
5. Man page, contextual help, redacted support bundle и cancellable progress
   для долгих операций не завершены.

## Реальная готовность платформ

| Платформа | Что есть | Что отсутствует | Допустимый статус |
|---|---|---|---|
| Linux CLI/TUI | Рабочие backend, rollback/recovery, installer/systemd и слитый partial API client | Остальные API domains, native peer identity, real-host crash/socket/upgrade gates | stable для 1.2.0; новый API — beta |
| Linux Desktop | Tauri control-center, DEB/RPM/AppImage preview, bundled bootstrap | Полная API migration, строгая privilege boundary, signing/update/rollback, clean-device gates | preview |
| Windows | Tauri UI собирается в CI | Windows Service, Wintun/WireGuard, ACL/DPAPI, signed installer, functional tests | UI preview |
| macOS | Tauri UI собирается в CI | Packet Tunnel Network Extension, entitlements, Keychain/App Groups, signing/notarization, functional tests | UI preview |
| Android | План и issue #13 | Всё приложение и VPN backend | planned |
| iOS | План и issue #14 | Всё приложение, extension и store pipeline | planned |

## Как должны устанавливаться зависимости

Полностью автоматическая установка системных VPN-компонентов одной универсальной
командой невозможна и небезопасна. Нужны platform-native пакеты:

- **Linux:** DEB/RPM объявляют зависимости package manager; AppImage запускает
  отдельный подписанный, явно подтверждённый privileged bootstrap. Installer
  использует только pinned source commits и проверяет post-install состояние.
- **Windows:** signed MSIX/MSI/EXE ставит Windows Service и подписанный tunnel
  driver/backend через elevation. Приложение не скачивает произвольные
  исполняемые файлы при первом запуске.
- **macOS:** signed app bundle включает Network Extension. Установка проходит
  через системное разрешение extension; Developer ID build notarized и stapled.
- **Android:** backend библиотека входит в signed AAB/APK. Разрешение VPN
  выдаётся через системный `VpnService.prepare`; нативные `.so` собираются ABI
  matrix в Gradle.
- **iOS:** Packet Tunnel extension входит в signed archive с правильными
  entitlements/App Group. Зависимости встраиваются на этапе Xcode build; runtime
  installer запрещён моделью iOS.

## Обязательная архитектура по платформам

### Windows

- service под Service Control Manager с минимальным IPC и restrictive ACL;
- WireGuard/Wintun как первый backend, DPAPI/Credential Manager для секретов;
- signed/timestamped installer и все бинарные компоненты;
- install/upgrade/uninstall/rollback tests на чистых Windows VM;
- route, DNS, leak, crash, sleep/resume и network-change integration tests.

### macOS

- `NEPacketTunnelProvider`/Network Extension с Apple entitlement;
- App Group IPC и Keychain, без root shell adapter;
- hardened runtime, Developer ID signing, notarization и stapling;
- тесты на реальном Mac: extension approval, upgrade, sleep/wake, DNS/routes.

### Android

- native `VpnService`: consent, foreground notification, `Builder.establish`,
  `protect()` для tunnel sockets и корректный `onRevoke`;
- WireGuard engine, Android Keystore и Storage Access Framework;
- always-on/lockdown, Doze, reboot, network-change и upgrade tests;
- signed AAB, Play App Signing, актуальный target API и Data safety disclosure.

### iOS

- Packet Tunnel Network Extension, Keychain и App Group;
- Apple Developer organization account и VPN entitlements;
- on-demand lifecycle, network-change/background recovery;
- real-device tests, TestFlight, privacy manifest/disclosures, export compliance
  и App Review.

## Проверки аудита

Локально подтверждены:

- Bash syntax и ShellCheck;
- Rust 1.85.0: format, Clippy с `-D warnings` и 15 unit tests;
- strict API contract: 25 операций и 14 error codes;
- capability registry: 14 capabilities и 6 release gates;
- `npm audit --audit-level=high`: 0 известных vulnerabilities;
- SHA-256 всех пяти локальных ignored-артефактов в `dist/`: четыре проверены
  общим `SHA256SUMS`, архив 1.0.0 — отдельным sidecar manifest;
- shell regression suite: 67/67;
- release-mode Desktop binary, DEB, RPM и AppImage; DEB содержит base runtime,
  `pkexec` и auto-detected WebKit/GTK dependencies, RPM — base runtime,
  `polkit` и library requirements; отдельный package audit проверяет
  engine/API/unit/drop-in byte identity, scriptlets, отсутствие `/usr/local` и
  отсутствие package-owned `/etc/vpnctl`;
- AppImage успешно собран локально; его подготовленный AppDir byte-match source,
  но clean-builder install/run test и reproducibility всё ещё не выполнены;
- PR #26/#27 слиты после зелёных required checks; post-merge CI на точном
  `main` SHA `52e695f` также зелёный. Package-lifecycle branch требует нового
  полного CI после собственных commits.

Не проверено и не должно подразумеваться:

- реальное VPN-подключение на чистом Linux host;
- Windows/macOS VPN lifecycle;
- Android/iOS build или lifecycle;
- production signing, notarization, stores, upgrade/rollback;
- воспроизводимая AppImage-упаковка на чистом builder;
- Cargo/system dependency advisory audit и production release trust chain.

Каталог `dist/` целиком исключён через `.gitignore`: это локальный cache
артефактов, а не tracked source/release manifest. Его файлы не удалялись.

## Порядок дальнейшей работы

1. Завершить и влить #26: recovery, bounded transport/journal, real systemd
   integration tests и review privilege boundary.
2. Перебазировать и влить #27: стабильный machine JSON, CLI/TUI API transport и
   parity tests.
3. Расширить API безопасными status/service/profile/test domains и убрать
   `pkexec` из Desktop operation paths.
4. Вынести root logic из Bash в небольшой native service с peer credentials,
   streaming limits и общей state machine.
5. Закрыть Linux clean-install/upgrade/uninstall/fault gates и только затем
   готовить Desktop Linux 1.0.
6. Делать Windows и macOS backend независимо; UI build не считать backend.
7. После стабилизации profile/core contract начать отдельные Android/iOS
   proof-of-concept и не смешивать mobile release с Desktop release.
